# --- Core causal discovery ----------------------------------------------------
# 0.1.4.0+ is required for NumPy 2.x compatibility.
causal-learn>=0.1.4.0

# causal-learn lists these as REQUIRED in its docs but they don't always
# install transitively (pydot in particular fails silently on Windows when
# Graphviz isn't on PATH). Pin them here so pip is forced to surface the error.
statsmodels>=0.14
pydot>=2.0

# Standalone LiNGAM package. NOT provided by causal-learn: bootstrap_ci.py
# imports DirectLiNGAM and ICALiNGAM from `lingam` directly, so omitting this
# breaks `python bootstrap_ci.py` with an ImportError. Tested with 1.12.2.
lingam>=1.12

# --- Scientific stack ---------------------------------------------------------
# Lower bounds match what the paper / causal-learn use; upper bound on numpy
# protects against breakage if a future major bump arrives. Adjust freely.
numpy>=1.23,<3.0
pandas>=1.5
scipy>=1.10
scikit-learn>=1.2

# --- Plotting & utilities -----------------------------------------------------
matplotlib>=3.6
networkx>=2.8
tqdm>=4.65

# Python wrapper for Graphviz, used by visualization.py for every DAG figure.
# This package alone is NOT sufficient: visualization.py also checks for the
# `dot` executable (shutil.which("dot")) and raises with install instructions
# if the Graphviz system binaries are missing. Install those separately:
#     Windows : https://graphviz.org/download/  (tick "Add to PATH")
#     macOS   : brew install graphviz
#     Linux   : sudo apt install graphviz
graphviz>=0.20